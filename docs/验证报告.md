# 验证报告

验证日期：2026-04-20

## 已验证命令

```bash
npm run verify:offline
npm install --ignore-scripts
npm run typecheck
npm run web:build
npm run api:hono:build
npm run build
```

## 已验证服务

### Hono

```bash
cd apps/api-hono
npm run dev
curl http://localhost:8787/health
```

返回：

```json
{"ok":true,"service":"dhp-api-hono"}
```

### FastAPI

```bash
cd apps/api-fastapi
python -m py_compile app/main.py
python -m uvicorn app.main:app --port 8000
curl http://127.0.0.1:8000/health
```

返回：

```json
{"ok":true,"service":"dhp-api-fastapi"}
```

## 说明

- ZIP 包不包含 `node_modules` 和 `.next` 构建产物。
- `package-lock.json` 已保留，便于复现安装。
