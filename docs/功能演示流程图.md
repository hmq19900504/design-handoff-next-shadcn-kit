# DHP 功能演示与流程图

## 一、核心理念：为什么需要 DHP

```
传统流程（Agent 不稳定）：
  设计截图 → Agent 看图猜 UI → 不可控的代码

DHP 流程（Agent 可约束）：
  设计意图 → 结构化协议 → 校验通过 → Agent 按约束生成 → 可验证的代码
```

DHP 不是让 Agent "更聪明"，而是让 Agent "更受控"。

---

## 二、整体架构

```mermaid
graph LR
    subgraph "人类工作区"
        A["产品原型 / UI 设计稿"] --> B["DHP 协议包"]
    end

    subgraph "机器工作区"
        B --> C["dhp:validate"]
        C --> D["dhp:build"]
        D --> E["dhp:sync"]
        E --> F["Codex Skill 读取 bundle"]
        F --> G["生成 Next.js / Hono 代码"]
    end

    subgraph "验证区"
        G --> H["typecheck"]
        H --> I["web:build"]
        I --> J["运行 & 浏览器验收"]
    end

    style B fill:#fef3c7,stroke:#d97706
    style E fill:#d1fae5,stroke:#16a34a
    style J fill:#dbeafe,stroke:#2563eb
```

---

## 三、协议包结构与数据流

```mermaid
graph TB
    subgraph "handoff/ 协议包"
        HJ["handoff.json<br/>总入口"] --> Pages["pages/*.md<br/>页面协议"]
        HJ --> Comps["components/*.md<br/>组件契约"]
        HJ --> Flows["flows/*.md<br/>交互流程"]
        HJ --> Tokens["tokens/design-tokens.json<br/>31 个视觉 token"]
        HJ --> CMap["mapping/component-map.shadcn.json<br/>6 个组件映射 + 禁用规则"]
        HJ --> API["api/api-contract.json<br/>4 个 endpoint + 4 个 model"]
        HJ --> Accept["acceptance/checklist.md<br/>验收清单"]
    end

    subgraph "构建产物"
        Pages --> Bundle["handoff.bundle.json<br/>机器可读单文件"]
        Comps --> Bundle
        Flows --> Bundle
        Tokens --> Bundle
        CMap --> Bundle
        API --> Bundle
        Bundle --> GenJSON["apps/web/src/generated/<br/>handoff.bundle.json"]
        Tokens --> GenCSS["apps/web/src/generated/<br/>dhp-tokens.css"]
    end

    style HJ fill:#fef3c7
    style Bundle fill:#d1fae5
    style GenJSON fill:#dbeafe
    style GenCSS fill:#dbeafe
```

---

## 四、校验 → 构建 → 同步 三步流水线

### Step 1: `npm run dhp:validate`

校验器检查协议完整性，任何缺失都会报错中断：

```
检查项                          规则
─────────────────────────────────────────────────
handoff.json 必填字段           dhpVersion, module, targetStack, tokens,
                                componentMap, pages
design-tokens.json              至少 8 个 token，不允许空值
component-map.shadcn.json       必须有 uiLibrary 和 components
pages/*.md                      每个页面必须有 dhp-layout + dhp-state-matrix
components/*.md                 每个组件必须有 dhp-component
```

实际输出：

```
DHP validate passed: handoff
- module: merchant-risk-console
- pages: 1
- components: 5
- tokens: 31
```

### Step 2: `npm run dhp:build`

把分散的协议文件打包成单个 `handoff.bundle.json`：

```mermaid
graph LR
    A["handoff.json"] --> Build["dhp-build.mjs"]
    B["design-tokens.json"] --> Build
    C["component-map.shadcn.json"] --> Build
    D["api-contract.json"] --> Build
    E["pages/*.md → 提取 dhp-* 块"] --> Build
    F["components/*.md → 提取 dhp-* 块"] --> Build
    G["flows/*.md → 提取 dhp-* 块"] --> Build
    Build --> H["handoff.bundle.json"]
    style H fill:#d1fae5
```

### Step 3: `npm run dhp:sync`

同步到 Next.js 应用的 `src/generated/` 目录：

- 复制 `handoff.bundle.json` → 前端可 import
- 从 tokens 生成 `dhp-tokens.css`（31 个 CSS 变量）

生成的 CSS 变量示例：

```css
:root {
  --dhp-color-background: #F8FAFC;
  --dhp-color-primary: #2563EB;
  --dhp-color-danger: #DC2626;
  --dhp-spacing-lg: 16px;
  --dhp-radius-md: 10px;
  --dhp-shadow-card: 0 1px 2px rgba(15, 23, 42, 0.06);
  /* ... 共 31 个 */
}
```

---

## 五、Agent 约束模型

```mermaid
graph TB
    subgraph "Agent 决策树"
        Start["读取 handoff.bundle.json"] --> Q1{"组件在 component-map 中?"}
        Q1 -->|"是"| Q2{"视觉值来自 tokens?"}
        Q1 -->|"否"| Reject1["❌ 拒绝生成，要求补充映射"]
        Q2 -->|"是"| Q3{"覆盖了 4 种状态?"}
        Q2 -->|"否"| Reject2["❌ 拒绝硬编码，引用 token"]
        Q3 -->|"是"| Q4{"数据字段来自 api-contract?"}
        Q3 -->|"否"| Reject3["❌ 补充 loading/empty/error/no-permission"]
        Q4 -->|"是"| Generate["✅ 生成代码"]
        Q4 -->|"否"| Reject4["❌ 不能自行发明字段"]
    end

    style Generate fill:#d1fae5
    style Reject1 fill:#fee2e2
    style Reject2 fill:#fee2e2
    style Reject3 fill:#fee2e2
    style Reject4 fill:#fee2e2
```

Agent 的 5 条硬约束：

| # | 约束 | 违反后果 |
|---|------|---------|
| 1 | 只能用 component-map 中声明的组件 | 拒绝生成 |
| 2 | 视觉值必须来自 tokens | 拒绝硬编码 |
| 3 | 每个页面必须覆盖 loading/empty/error/no-permission | 补充后才能继续 |
| 4 | 数据字段必须来自 api-contract | 不能自行发明 |
| 5 | 缺失信息必须显式写假设 | 不允许静默补全 |

---

## 六、示例：从协议到代码的完整映射

### 协议侧（FilterPanel 组件契约）

```json
{
  "name": "FilterPanel",
  "implementedBy": ["Card", "Input", "Select", "Button"],
  "fields": [
    {"name": "keyword", "label": "商户/风险ID", "component": "Input"},
    {"name": "riskLevel", "label": "风险等级", "component": "Select"},
    {"name": "source", "label": "风险来源", "component": "Select"},
    {"name": "dateRange", "label": "时间范围", "component": "Select"}
  ],
  "states": ["idle", "loading", "error"]
}
```

### 代码侧（生成的 React 组件）

```tsx
function FilterPanel({ query, setQuery, loading }) {
  return (
    <Card>
      <CardContent>
        <div className="grid gap-3 md:grid-cols-4">
          <label>商户/风险ID
            <Input disabled={loading} value={query.keyword} />
          </label>
          <label>风险等级
            <Select disabled={loading} value={query.riskLevel}>...</Select>
          </label>
          <label>风险来源
            <Select disabled={loading} value={query.source}>...</Select>
          </label>
          <label>时间范围
            <Select disabled={loading} value={query.dateRange}>...</Select>
          </label>
        </div>
        <Button disabled={loading}>{loading ? "查询中…" : "查询"}</Button>
      </CardContent>
    </Card>
  );
}
```

映射关系：
- `implementedBy: ["Card", "Input", "Select", "Button"]` → 只用了这 4 个 shadcn/ui 组件
- `fields[].component` → 直接对应 JSX 元素
- `states: ["loading"]` → `disabled={loading}` + 按钮文案切换

---

## 七、前后端数据契约联动

```mermaid
sequenceDiagram
    participant 协议 as api-contract.json
    participant Hono as Hono API
    participant Web as Next.js 前端
    participant User as 运营用户

    协议->>Hono: 定义 RiskItem 模型 + 4 个 endpoint
    协议->>Web: 定义 TypeScript 类型 + 请求参数

    User->>Web: 修改筛选条件
    Web->>Hono: GET /api/risk-items?riskLevel=high
    Hono-->>Web: RiskItem[]
    Web-->>User: 渲染 RiskTable

    User->>Web: 点击"查看"打开详情
    User->>Web: 提交处置
    Web->>Hono: POST /api/dispositions {riskId, action, reason}
    Hono-->>Web: {ok: true, message: "已提交处置：restrict"}
    Web-->>User: 更新行状态为 processing
```

API 契约定义的 4 个数据模型：

```
RiskItem      → 风险对象（表格行）
Metric        → 指标卡数据
RiskQuery     → 筛选参数
DispositionRequest → 处置请求体
```

---

## 八、状态矩阵演示

页面协议中定义的 5 种状态，前端必须全部实现：

```mermaid
stateDiagram-v2
    [*] --> loading: 页面初始化
    loading --> success: 数据返回
    loading --> error: 请求失败
    success --> empty: 筛选无结果
    empty --> success: 清空筛选
    error --> loading: 点击重试
    success --> no_permission: 权限不足

    state loading {
        [*] --> 骨架屏
        note right of 骨架屏: 筛选区 1 行\n指标卡 4 张\n表格 6 行
    }

    state empty {
        [*] --> 空态文案
        note right of 空态文案: "暂无符合条件的风险对象"\n操作：清空筛选
    }

    state error {
        [*] --> 错误提示
        note right of 错误提示: 错误提示 + 重试按钮\n不清空已有筛选条件
    }

    state no_permission {
        [*] --> 权限提示
        note right of 权限提示: 禁止展示处置按钮\n仅显示权限提示
    }
```

前端实现中，用户可以通过顶部按钮切换 5 种状态来验证覆盖度。

---

## 九、Codex Skill 执行流程

```mermaid
graph TB
    subgraph "用户触发"
        U["请使用 design-handoff-next-shadcn，<br/>读取 handoff.json，生成页面"]
    end

    subgraph "Skill 执行"
        U --> S1["1. 检查 bundle 是否存在"]
        S1 -->|"不存在"| S2["执行 validate → build → sync"]
        S1 -->|"存在"| S3["2. 读取 handoff.bundle.json"]
        S2 --> S3
        S3 --> S4["3. 解析 tokens + component-map + api-contract"]
        S4 --> S5["4. 逐页面提取 dhp-layout / dhp-state-matrix"]
        S5 --> S6["5. 逐组件提取 dhp-component"]
        S6 --> S7["6. 生成/修改 Next.js 页面"]
        S7 --> S8["7. 生成/修改 Hono API"]
        S8 --> S9["8. 运行 typecheck + build"]
        S9 --> S10["9. 输出完成项 / 假设项 / 未完成项"]
    end

    style S3 fill:#d1fae5
    style S10 fill:#dbeafe
```

---

## 十、一键验证命令

```bash
# 最小验证（无需 npm install）
npm run verify:offline

# 完整验证
npm install
npm run typecheck
npm run web:build
npm run api:hono:build
```

验证通过意味着：
- 协议格式正确
- Bundle 可构建
- TypeScript 类型安全
- Next.js 生产构建成功
- Hono API 可编译

---

## 十一、项目运行效果

启动后访问 `http://localhost:3000`，可以看到：

1. 顶部标题栏：显示 DHP Bundle 版本和模块名
2. 状态切换按钮：success / loading / empty / error / no-permission
3. 筛选区：4 个字段（商户ID、风险等级、风险来源、时间范围）
4. 指标卡：4 张（总风险数、高危占比、待处理、本周新增）
5. 风险列表：Table + Badge + 操作按钮
6. 详情抽屉：Sheet 侧滑，展示详情 + 处置表单
7. 协议摘要：展示 bundle 中的页面/组件/流程数量

每个 UI 区块都严格对应协议中的一个 `dhp-component`，没有任何"自由发挥"的部分。
